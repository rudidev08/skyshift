import type { MapPreset } from "./map-types";

export const blankPreset: MapPreset = {
  id: "blank",
  name: "Blank",
  description: "Empty universe — every zone starts buildable.",
  stations: [],
};

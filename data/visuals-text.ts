// Visual-only text constants for map-space rendering — the font stacks and
// label color shared by every Phaser text label. Mirrors the ui.css
// --font-display / --font-mono custom properties (kept in sync by hand).

/** Display font stack — station/ship names, sector labels. */
export const displayFontFamily = '"Space Grotesk", system-ui, sans-serif';

/** Monospace font stack — trade-count overlays and other numeric readouts. */
export const monoFontFamily = '"JetBrains Mono", ui-monospace, monospace';

/** Default grey for map-space labels. */
export const mapLabelColor = "#c0c0c0";
